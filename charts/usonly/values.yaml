replicaCount: 1

image:
  repository: oink12268/usonly
  tag: "18"
  pullPolicy: Always

service:
  type: ClusterIP
  port: 8080

ingress:
  enabled: true
  host: usonly.iptime.org
  tlsSecretName: usonly-tls

mysql:
  host: mysql
  port: 3306
  database: usonly
  username: root
